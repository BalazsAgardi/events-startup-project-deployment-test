version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
      # Add your database configuration here
      # DB_HOST: localhost
      # DB_USER: root
      # DB_PASSWORD: password
      # DB_NAME: events_db
